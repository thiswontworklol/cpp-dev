#include <iostream>
#include <cmath>
using namespace std;
int main(){
    double a,b;
    cout << "Enter the power";
    cin >> b;
    cout << "Enter the number";
    cin >> a;
    cout << pow(a,b);
}